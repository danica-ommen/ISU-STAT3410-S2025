To run the app, enter the following command into the R console:
shiny::runGitHub("danica-ommen/ISU-STAT3410-S2025", subdir = "Lab0")

If you have not done so already, you may need to install and load the shiny package:
install.packages("shiny")
library(shiny)
